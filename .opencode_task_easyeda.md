# AUFGABE: EasyEDA-Baugeneratoren auf die neue Netzliste ziehen (45 Netze / 64 Positionen)

## Kontext

Zwei Änderungen sind im Repo fertig und abgenommen:
1. **Lichtsensor** (J7, R_LIGHT, R_LIGHT_S, C_LIGHT, Netz `LIGHT_RAW`/`LIGHT_AOUT`, ADC auf IO4)
2. **Pinordnung + Erweiterung** (J2/J7 auf GND–VCC–SIG; neu: J8 = I²C 4-pol mit Load-Switch Q2
   und Rail `VCC_EXT`, J9 = Reserve-Analog IO5, J10 = Reserve-Digital IO21, TP7–TP11)

Stand der Quelle `hardware/schaltplan_v1_netzliste.csv`: **45 Netze, 64 bestückte Positionen**
(mit den Lötpads/Bohrungen dazwischen 187 Zeilen). Prüfer: `check_netlist.py` Exit 0,
`check_bom_consistency.py` Exit 0, `design/report.py` 29/29.

Der eingecheckte **EasyEDA-Bau ist ein Schnappschuss vom 11.09.2026 (30 Netze, 54 Bauteile)** und
muss laut `hardware/easyeda/README.md` aus der aktuellen Netzliste **neu erzeugt** werden.

## Aufgabe (NUR Dateien erzeugen — den Editor NICHT anfassen)

Erweitere die Generatoren unter `hardware/easyeda/scripts/` so, dass sie mit der neuen Netzliste
laufen, und **erzeuge die Artefakte neu**:

| Skript | Erwartung |
|---|---|
| `build_ir.py` | liest die Netzliste → `raw/ir_draft.json`; muss die neuen Bauteile/Netze abbilden (Symbol-/Library-Zuordnung ergänzen: 4-pol JST-XH `C157925`, P-Kanal-MOSFET `C15127`, Lötpads TP7–TP11 wie die bestehenden TP1–TP6) |
| `plan_layout.py` | Modulblöcke für **zwei neue Module** (LICHT, ERWEITERUNG) + J2-Umstellung → `raw/place_all.sh`, `raw/frames_*.json` |
| `build_autoconnect.py` | Autoconnect-Specs je Modul → `raw/ac_<MODUL>.json`, inkl. der neuen Netze |
| `raw/designator_changes.json` | neue funktionale Namen → **numerische** Designatoren fortschreiben (Muster: `R_LIGHT` → `R17`, `R_LIGHT_S` → `R18`, `C_LIGHT` → `C15`, `R_SDA_PU` → `R19`, … lückenlos und kollisionsfrei; bestehende 16 Zuordnungen unverändert lassen) |

**Wichtig — Reihenfolge und Konsistenz:**
- Die Zuordnung muss zur bestehenden Seite passen (R1–R16, C1–C14, LED1, TP1–TP6, J1/J2/J4/J5/J6, U1/U3/U4/U6/U7 sind vergeben).
- Der **Lötpad-Sonderfall**: TP7–TP11 sind keine BOM-Position und haben keinen Bestückungsplatz.
- **J2-Pinordnung** ist gedreht (1 = GND, 2 = SENSOR_PWR, 3 = SENSOR_RAW) — die Netzliste ist die Quelle, der Generator muss das 1:1 übernehmen, nicht die alte Reihenfolge hardcodieren.

## Zusätzlich zwei veraltete Dateien nachziehen

- `hardware/easyeda/s0_spec.json` — Netzzahl/Modulliste auf den echten Stand (45 Netze, Module LICHT + ERWEITERUNG)
- `hardware/pcba_verfuegbarkeit_jlc.md` — Zeilen für **J8 (JST-XH 4-pol, `C157925`, 89.223 JLC-Bestand, $0,0863@10)** und **Q2 (AO3401A, `C15127`, basic, 591.277 JLC-Bestand, $0,0908@10, RDS(on) 85 mΩ @ VGS −2,5 V)** ergänzen; die LCSC-Retailbestände weichen ab (1.831.820 bzw. 4.435) — beide Zahlen mit Quelle nennen, für die PCBA zählt der JLC-Bestand.

## Nachweis (muss belegt werden)

1. Alle Generatoren laufen mit `/Users/manuel/.venvs/…`-freiem Standard-Python (`python3`) durch, Exit 0.
2. **Selbstkonsistenzprüfung:** ein Vergleich, der die erzeugte IR (`raw/ir_numbered.json`) Pin für Pin
   gegen `hardware/schaltplan_v1_netzliste.csv` stellt → **0 Abweichungen**, und die Anzahl Netze/Pins ausgeben.
3. Kurzbericht: Zahl der Module, Netze, Bauteile; Liste der neuen Designator-Zuordnungen; alles, was nicht sauber aufging (ehrlich, nicht schöngerundet).

## Regeln

- **Keine Live-Aufrufe der `easyeda`-CLI.** Nur Dateien erzeugen; die Editor-Schritte mache ich selbst.
- Keine LCSC-Codes erfinden — die genannten sind live geprüft.
- Nur Dateien unter `hardware/easyeda/` (plus die zwei genannten Doku-Dateien) ändern.
- Einfachster prozeduraler Code, keine Frameworks.
