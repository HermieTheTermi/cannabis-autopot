# AUFGABE: Design-Prüfsuite (`hardware/design/`) auf die 2S-Schaltung umstellen

## Lage (16.09.2026)

Der Schaltplan wurde von **1S + Boost + LDO** auf **2S + zwei Abwärtswandler** umgebaut.
Verbindliche Quellen (NICHT ändern, nur lesen):

* `hardware/schaltplan_v1.md` — §1–§6 neuer Stand, §13 = Revisionsliste, §7–§12 Historie
* `hardware/schaltplan_v1_netzliste.csv` — 68 Netze, 115 Bauteile, 314 Verbindungen
* `hardware/pcba_bom_jlc.csv` — 109 bestückte Positionen
* `hardware/bom_entscheidung.md` — Bauteilentscheidungen (Pumpe, Akku, Sensor)
* `docs/11_review-2s-umbau.md` — das Review zum Umbau (Zahlen, Begründungen)

**Zustand der Suite:** sie läuft derzeit **gar nicht**.
`python3 hardware/design/report.py` bricht ab mit `Bauteil nicht in schaltplan_v1.md: R_PROG`;
`circuit.py` bricht in `load_system()` ab (`Kennwert 'Zellspannung' nicht gefunden`), weil die
Prosa-Regexe noch auf die 1S-Dokumente zeigen. Zusätzlich sind mehrere Prüfungen inhaltlich
überholt (MCP73831, ME6211, MT3608, MAX809).

## Ziel

`python3 hardware/design/report.py` läuft **sauber durch (Exit 0)** und prüft die **2S-Schaltung**
gegen die Datenblattgrenzen. Nur Standardbibliothek, Python 3.9+.

## Harte Regeln (nicht verhandelbar)

1. **Nur `hardware/design/*.py` ändern** (circuit.py, checks.py, sim.py, report.py, README.md dort).
   `schaltplan_v1.md`, `schaltplan_v1_netzliste.csv`, `pcba_bom_jlc.csv`, `bom_entscheidung.md`,
   `docs/*` **nicht anfassen**.
2. **Bauteilwerte werden GELESEN, nicht wiederholt.** Jeder Wert, der in einer Bauteiltabelle
   steht, ausschließlich über `circuit.part("<Designator>")` holen (die Tabellen in §3.1–§3.4
   enthalten alle Positionen mit Wert und LCSC).
3. **Nur Datenblattgrenzen dürfen im Code stehen**, jede mit Quellenkommentar
   (Datenblatt + Abschnitt). Systemgrößen (Pumpe, Pack, Modul, ADC) als ein klar markierter
   `SYSTEM`-Block mit Quelle — und mit einer Prüfung, die nachweist, dass diese Zahlen
   **wirklich in den Dokumenten vorkommen** (siehe `check_system_quellen`).
4. **Niemals still bestehen**: Passt ein Muster/Wert nicht, laut abbrechen (`CircuitError`) —
   eine Prüfung, die bei fehlendem Wert „grün" meldet, ist schlimmer als keine.
5. Keine Tautologien: eine Prüfung darf ihr Soll nicht aus derselben Zahl ableiten, die sie prüft.
6. Am Ende selbst laufen lassen und den Exit-Code prüfen; die Ausgabe muss jede Prüfung mit
   **Name · Ist · Soll · Begründung** zeigen.

## Kennwerte für die Prüfungen (Datenblatt-Anker, damit nichts geraten wird)

### Lader IP2326 (`U_CHG`, LCSC C2832094)
* Ladestrom **ICHG = 90000 / R_ISET** (Datenblatt V1.11 §„充电电流设置", S. 11). R_ISET ist im Plan **100 kΩ**.
  Grenzen: 0 … **1,5 A** (§7 Elektrische Eigenschaften, S. 4). ISET darf **nicht offen** sein (§4 Pin-Beschreibung).
* Ladeschluss: **VSET offen ⇒ 8,4 V** (Toleranz 8,3–8,5 V), §7/S. 4. ⚠️ Variante `IP2326_8V8` = 8,8 V → darf **nicht** bestellt sein (Textprüfung).
* Ladephasen (§9, S. 9): < 3,7 V → 50 mA · 3,7–6 V → 100 mA · > 6 V → ICC · ab ~8,4 V CV · Stopp bei 200–300 mA.
* Wirkungsgrad **94 %** bei 5 V → 8 V/1 A (§1, S. 1). Eingang 4,5–9,5 V (S. 3).
* CON_SEL offen ⇒ **2S** (§4/S. 8).

### 5-V-Buck SY8113B (`U_BUCK5`, C78989) — Datenblatt AN_SY8113B
* **V_out = 0,6 V × (1 + R_FB5_TOP/R_FB5_BOT)**, V_REF 0,6 V ±1,5 % (S. 1/S. 2).
* 3 A Ausgang (S. 1), Eingang 4,5–18 V, **f = 500 kHz**, Iq 100 µA, Shutdown 5–10 µA.
* EN: **High-Schwelle 1,5 V**, Low 0,4 V; Input-UVLO 4,5 V; „Do not float".
* Stromgrenzen: **Valley-Limit 3,0 … 4,25 A**, Top-FET-Peak 6 A (Elektrische Eigenschaften).
* R_DS(on) 80 mΩ (high) / 40 mΩ (low), θ_JA 100 °C/W, Sanftanlauf 800 µs.
* Induktivität: **L_BUCK5 = 4,7 µH, Isat 4,0 A** (Herstellerdatenblatt PRS6045), Rippel
  ΔI = (V_in − V_out) × D / (L × f) mit D = V_out/V_in.

### 3,3-V-Buck AP63203 (`U_BUCK3`, C780769) — Datenblatt DS41326
* **V_out = 0,8 V × (1 + R_FB3_TOP/R_FB3_BOT)**, V_REF 0,8 V ±1 %.
* 2 A, 3,8–32 V, **f = 1,1 MHz**, **Iq 22 µA**, θ_JA 89 °C/W, EN „Do not float".
* Zielbereich des Moduls (Espressif ESP32-C6-MINI-1): **V_DD33 3,0 … 3,6 V**.

### Unterspannungswächter TPS3839G33 (`U7`, C485802) — Datenblatt SBVS193D
* **V_IT = 3,003 … 3,126 V** (negativ), Hysterese 31 mV, **Iq 150 nA typ / 500 nA max**.
* V_DD 0,9 … 6,5 V; Ausgang **Push-Pull**, **V_OL ≤ 0,4 V bei I_OL = 2 mA** (2,8–6,5 V),
  **V_OH ≥ V_DD − 0,4 V**, Reset-Delay **200 ms**.
* Beschaltung im Plan: Teiler **R3a/R3b = 200 kΩ/200 kΩ** (Versorgung = VBAT/2),
  ⇒ Auslösung bei **V_trip = V_IT × (1 + R3a/R3b) + Iq × R3a**.
* Ziel: Abschaltung **2 × 3,0 V … 2 × 3,3 V** = **6,0 … 6,6 V** Packspannung.

### Klemmzweig (der wichtigste Regressionsschutz)
Der Klemmzweig **muss eine Serienkette** sein:
`GATE (bzw. GATE2) → R_CLAMP1/2 (10 kΩ) → D3/D8 → RESET_UV (U7 Pin 2)`.
Im Altstand lagen R_CLAMP und die Diode **parallel** und der Knoten hing an keinem Gate —
die Unterspannungsklemmung war wirkungslos (Befund 1 in `docs/11_review-2s-umbau.md`).
Diese Prüfung muss genau das erkennen können (siehe `check_klemmzweig_serie`).

### ADC (Espressif ESP32-C6, ADC1)
* 12 Bit, Bereich `ADC_ATTEN_DB_12` = **0 … 3300 mV**.
* Teiler: R_SENSE_TOP **200 kΩ** / R_SENSE_BOT **68 kΩ**; V_ADC = VBAT × R_bot/(R_top+R_bot)
  ⇒ bei 8,4 V **2,13 V**; Soll: ≤ 3300 mV **und** ≥ 1000 mV (Auflösungsreserve).

### Systemgrößen (Belegstellen — in `check_system_quellen` gegen die Dokumente prüfen)
| Größe | Wert | Belegstelle |
|---|---|---|
| Pack 2S | 7,4 V nominal, **6,0–8,4 V** Bereich, **2000 mAh** | `hardware/schaltplan_v1.md` §2.2/§6.3, `hardware/bom_entscheidung.md` §1 Zeile 4 |
| Pumpe Dosier | **5 V**, 0,4 A Leerlauf, **3 A Anlauf** | `hardware/bom_entscheidung.md` §1 Zeile 2, §4c |
| Pumpe O2 (optional) | 5 V, 0,2 A | `hardware/bom_entscheidung.md` §8 |
| Modul | TX-Peak **382 mA**, Deep-Sleep **7 µA** | `hardware/bom_entscheidung.md` §6.2, `hardware/schaltplan_v1.md` §6.2 |
| Ruhestrom-Bausteine | SY8113 **100 µA**, AP63203 **22 µA**, TPS3839 **0,15 µA** | Datenblätter (oben) |
| Netzteilannahme | **5 V / 2,5 A** | `hardware/schaltplan_v1.md` §6.1 |

## Aufgabe im Einzelnen

### 1. `circuit.py`
* `load_system()` **neu schreiben**: keine brüchigen Prosa-Regexe für Bauteilwerte mehr.
  Bauteilwerte über `part()`, Systemgrößen über den markierten `SYSTEM`-Block.
* Helfer behalten/ergänzen: `parse_ohm/parse_volt/parse_ampere` (Werte aus den Tabellen),
  `ratio(designator)` = R_top/R_bot, `net_of()`, `parts_on(net)`.
* Fehlt ein Bauteil oder ein Wert: `CircuitError` mit klarer Meldung.

### 2. `checks.py` — zu ersetzen (1S-Reste, alle lesen Werte, die es nicht mehr gibt)
`check_ladestrom` (MCP73831/R_PROG), `check_laderkondensatoren` (4,7 µF),
`check_max809_klemmstrom`, `check_ldo_reserve`, `check_ldo_headroom`.
Außerdem: die statische Kanal-Liste enthält noch **J14** — J14 ist seit 15.09.2026 entfernt
(IO22 ist jetzt PUMP2_EN/J16); Eintrag entfernen.

### 3. `checks.py` — neu (Name → Formel → Soll → Quelle)
| Prüfung | Formel / Inhalt | Soll | Quelle |
|---|---|---|---|
| `check_ladestrom_ip2326` | ICHG = 90000/R_ISET | ≤ 1,5 A **und** ≤ 1 C des Packs | IP2326 S. 4/S. 11 |
| `check_ladeschluss_2s` | 2 × 4,2 V gegen VSET-offen-Wert + Textprüfung „kein IP2326_8V8 im BOM" | 8,4 V ±0,1 V, kein 8V8 | IP2326 S. 4/S. 5 |
| `check_ladeeingang_strom` | I_in = V_out·I_CHG/(η·V_USB) + Systemlast | ≤ 2,5 A (Netzteilannahme) | IP2326 S. 1 (94 %) |
| `check_buck5_ausgang` | 0,6 × (1 + R_FB5_TOP/R_FB5_BOT) | 5,0 V ±5 % (Pumpennennspannung) | SY8113B S. 1/S. 2 |
| `check_buck3_ausgang` | 0,8 × (1 + R_FB3_TOP/R_FB3_BOT) | 3,0 … 3,6 V | AP63203 + Espressif |
| `check_buck5_induktivitaet` | I_peak = I_Last + ΔI/2, ΔI = (V_in−V_out)·D/(L·f), D = V_out/V_in; V_in = 8,4 V **und** 6,16 V | I_peak < 4,0 A (Isat), I_Last ≤ 3 A | PRS6045 + SY8113B S. 1 |
| `check_buck3_induktivitaet` | dito mit 1,1 MHz | I_peak < 4,0 A | PRS6045 + AP63203 |
| `check_uvlo_schwelle` | V_trip = V_IT·(1+R3a/R3b) + Iq·R3a | 6,0 … 6,6 V | TPS3839 S. 7 |
| `check_waechter_sinkstrom` | 2 × (V_rail − V_F)/R_CLAMPx + EN-Leckstrom | ≤ 2 mA (V_OL = 0,4 V) | TPS3839 „Pin Functions"/EC |
| `check_adc_teiler_max` | V_ADC = VBAT_max · R_SENSE_BOT/(R_SENSE_TOP+R_SENSE_BOT) | ≤ 3,3 V **und** ≥ 1,0 V | Espressif ADC 12 dB |
| `check_buck_en_pegel` | V_OH_Wächter = V_DD − 0,4 V gegen EN-Schwelle 1,5 V | V_OH > 1,5 V | TPS3839 + SY8113B |
| `check_klemmzweig_serie` | Netzlistenprüfung: GATE→R_CLAMP1→D3→RESET_UV und GATE2→R_CLAMP2→D8→RESET_UV als **Serie** | genau diese Topologie | Review §5 Befund 1 |
| `check_kein_low_vin_am_vbat` | Kein Bauteil mit V_IN,max < 8,4 V darf auf dem Netz VBAT liegen (Liste der erlaubten VBAT-Teilnehmer + Datenblatt-V_IN,max je Teil) | nur Bauteile mit ausreichender Spannungsfestigkeit | ME6211-Lektion, Review §5 Befund 4 |
| `check_standby_budget` | Iq_Buck5 + Iq_Buck3 + Iq_Wächter + Teilerströme + Modul-Sleep | ≤ 250 µA; Ausgabe in mAh/Tag | Datenblätter |
| `check_system_quellen` | die Zahlenwerte des `SYSTEM`-Blocks müssen als Text in den Dokumenten vorkommen | jeder Wert belegt | Regel 3 oben |

### 4. `checks.py` — bestehende Prüfungen behalten und auf die neuen Rails ziehen
`check_gate_spannung`, `check_mosfet_verlust`, `check_freilaufdiode`, `check_teilerstrom`,
`check_uv_staffelung`, `check_adc_filter`, `check_light_*`, `check_led_*`, `check_btn_*`,
`check_pin_disziplin`, `check_stecker_pinordnung`, `check_erweiterung_*`, `check_load_switch_failsafe`,
`check_i2c_pullups`, `check_en_rc`, `check_netzstruktur`.
Anpassungen: Logikrail heißt weiterhin `+3V3` (jetzt 3,31 V statt 3,30 V), Pumpenrail `+5V`
(jetzt 5,10 V aus dem Buck statt Boost), `check_uv_staffelung` jetzt gegen **6,16 V** statt 3,08 V.
Rail-Spannungen **aus den Feedback-Teilern berechnen**, nicht abtippen.

### 5. `sim.py`
Rechenproben aktualisieren: Ladezeit (CC/CV grob) für 2000 mAh bei 0,90 A · Laufzeit je Dosiervorgang
aus der 5-V-Schiene · Teilerströme · Buck-Rippelströme · Standby in mAh/Tag. Getroffene Annahmen
im Code als Annahme kennzeichnen (z. B. Innenwiderstand des Packs) und in der Ausgabe nennen.

### 6. `report.py`
Mechanik behalten (Tabelle, Exit 1 bei jedem Fehlschlag). Nur wenn eine Prüfung entfällt, den Aufruf
entfernen. Die Ausgabe muss die neuen Prüfungen vollständig zeigen.

## Abnahme (bitte selbst nachweisen und im Bericht nennen)
1. `python3 hardware/design/report.py` → **Exit 0**, Anzahl Prüfungen und Zeilen ausgeben.
2. `python3 hardware/design/report.py; echo "exit=$?"` — Exit-Code wirklich prüfen.
3. Eine **Gegenprobe** zeigen (kurz im Bericht): einen Wert im Dokument versuchsweise ändern
   (z. B. R_FB5_TOP von 75 kΩ auf 100 kΩ im TEXT kopieren und wieder zurücksetzen) und belegen,
   dass die zugehörige Prüfung **rot** wird — danach zurücksetzen (Datei unverändert lassen!).
4. `python3 -m py_compile hardware/design/*.py` ohne Fehler.

## Bericht (kurz)
* Geänderte Dateien + je eine Zeile, was passiert ist
* Anzahl Prüfungen (vorher/nachher), Exit-Codes
* Welche Prüfungen entfallen sind und wodurch sie ersetzt wurden
* Alles, was NICHT umgesetzt werden konnte, mit Grund
