# AUFGABE: Design-Suite um die Prüfungen der Akku-Schutzbeschaltung erweitern

## Lage (16.09.2026)

Der Akku-Schutz sitzt jetzt **auf der Platine** statt in einem Fertigpack (Nutzer-Entscheidung:
"alles auf die Platine ... Ich brauche kein zweites extra Bord"). Neu im Schaltplan:

- `U_PROT` = **HY2120-CB** (SOT-23-6, LCSC `C116509`), 2-Zellen-Schutz-IC
- `Q_PROT1`, `Q_PROT2` = **PSMN4R2-30MLDX** (LFPAK33-8, `C179452`), Schalterpaar in der Minusleitung
- `R_PROT_VDD`, `R_PROT_VC` = 330 Ω · `R_PROT_CS` = 2 kΩ · `C_PROT_VDD`, `C_PROT_VC` = 100 nF · `R_CB` = 100 Ω
- `J1` ist jetzt **3-polig** (`C5258884`): 1 = BAT_MINUS, 2 = MID, 3 = VBAT
- Neues Netz **BAT_MINUS** (Pack-Minus zwischen Pack und Schalterpaar) + **MID** (Mittelabgriff)
- Die Balancing-Pins des Laders sind **erstmals verdrahtet**: `U_CHG` Pin 23 (VBATM) über `R_CB` an
  MID, Pin 24 (VBAT_GND) an BAT_MINUS

Die Dateien `hardware/schaltplan_v1.md`, `hardware/schaltplan_v1_netzliste.csv` und
`hardware/pcba_bom_jlc.csv` sind bereits aktualisiert und **nicht** mehr zu ändern (Lints laufen grün).
Deine Aufgabe betrifft **nur** `hardware/design/`.

## 1. Fehlschlagenden Bestandscheck reparieren

`python3 hardware/design/report.py` endet derzeit mit **36 von 37** und diesem Befund:

    - VBAT-Spannungsfestigkeit: Ist 13 Teilnehmer: ... R_PROT_VDD ... -> R_PROT_VDD nicht als
      VBAT-tauglich gelistet, Soll nur zugelassene Teilnehmer, V_IN,max >= 8,4 V

Ursache: `R_PROT_VDD` hängt jetzt neu an VBAT, ist aber in der Liste der zugelassenen VBAT-Teilnehmer
(V_IN,max je Bauteil) nicht geführt. Ergänze `R_PROT_VDD` (und falls nötig die weiteren neuen
Bauteile auf VBAT) mit **datenblattbelegtem** Bezug — 330 Ω 0805 ist ein Widerstand mit 50 V+;
schreibe die Begründung als Kommentar mit Quelle an die Liste. Wichtig: die Liste darf **nicht**
einfach pauschal erweitert werden — nur die wirklich geeigneten Teile.

## 2. Neue Prüfungen (jede liest echte Werte, keine Tautologien)

Alle Werte **müssen** aus `hardware/schaltplan_v1_netzliste.csv`, `hardware/schaltplan_v1.md`
oder aus belegten Datenblattkonstanten mit Quellenkommentar stammen — wie bei den bestehenden
Prüfungen. Fehlt ein Muster, laut abbrechen statt still bestehen.

1. **`check_schutz_serie`** (Topologie, Regressionsschutz — wichtigste Prüfung): In der Netzliste muss
   für **jeden** Zweig die Serienkette existieren:
   - J1 Pin 1 liegt auf demselben Netz wie `Q_PROT1` Pin 1/2/3 (Source) **und** `U_PROT` Pin 6 (VSS)
   - `Q_PROT1` "mb (Drain)" und `Q_PROT2` "mb (Drain)" liegen auf **demselben** Netz (PROT_COMMON)
   - `Q_PROT2` Pin 1/2/3 (Source) liegt auf **GND**
   - `U_PROT` Pin 1 (OD) **nur** an `Q_PROT1` Pin 4, `U_PROT` Pin 2 (OC) **nur** an `Q_PROT2` Pin 4
     (kein Tausch! OD = Entlader = packseitig)
   - `U_PROT` Pin 3 (CS) ist über `R_PROT_CS` mit GND verbunden (`R_PROT_CS` spannt auf zwei Netzen)
   - `U_PROT` Pin 5 (VDD) liegt über `R_PROT_VDD` an VBAT, `U_PROT` Pin 4 (VC) über `R_PROT_VC` an MID
   - `U_CHG` Pin 23 über `R_CB` an MID, `U_CHG` Pin 24 an BAT_MINUS
   - Gegenprobe einbauen: das Schalterpaar darf **nicht** auf GND liegen (dann wäre der Schutz wirkungslos)

2. **`check_schutz_schwellen`** (Abschaltstaffelung, Werte aus dem Dokument):
   - Überladung 4,28 V/Zelle ⇒ 8,56 V Pack muss **über** der Ladeschlussspannung des IP2326
     (8,4 V) liegen (sonst Störung im Normalbetrieb) und unter 8,8 V (kein Durchgehen)
   - Tiefentladung 2,90 V/Zelle ⇒ 5,80 V Pack muss **unter** der Wächterauslösung (6,19 V) und
     **über** 2,5 V/Zelle (= 5,0 V) liegen
   - Staffelung insgesamt: Firmware-Stopp (6,8 V) > Wächter (6,19 V) > Zellenschutz (5,80 V)

3. **`check_schutz_ueberstrom`** (Reserve gegen Fehlauslösung beim Pumpenanlauf):
   - Auslösestrom = V_DIP / (2 × R_DS(on,max)) = 200 mV / (2 × 5,7 mΩ) ≈ 17 A
   - Batterieseitiger Anlaufstrom der Pumpen = P_Pumpe / (η_Buck × V_Pack,min) — mit den im Dokument
     belegten Werten (Pumpe 5 V/3 A Anlauf, η 90 %, V_Pack,min 6,0 V) ⇒ ≈ 2,8 A
   - Forderung: Auslösestrom ≥ 2 × Anlaufstrom (Reserve); zusätzlich Spannungsabfall bei Dauerlast
     (0,65 A) berechnen und auf ≤ 50 mV prüfen

## Abnahme (alles selbst ausführen und belegen)

1. `python3 hardware/design/report.py` → **Exit 0, 40 von 40** (37 alt + 3 neu; Zählung anpassen)
2. `python3 -m py_compile hardware/design/*.py` → OK
3. **Gegenprobe je neuer Prüfung** (Datei vorher kopieren, danach exakt zurücksetzen):
   - Netzliste: `Q_PROT1` Source auf `GND` statt `BAT_MINUS` legen → `Schutz-Serienkette` muss FEHLER
   - Netzliste: OD und OC vertauschen (`U_PROT` Pin 1 an `Q_PROT2` Gate) → `Schutz-Serienkette` muss FEHLER
   - Dokument: Überstromwert 200 mV auf 50 mV senken → `Schutz-Ueberstrom` muss FEHLER
   - Dokument: Tiefentladung 2,90 V auf 3,40 V erhöhen → `Schutz-Schwellen` muss FEHLER
4. Kurz berichten: Prüfnamen wie im Bericht ausgegeben, gefundene Fallstricke, verwendete Quellen.

## Regeln

- Nur `hardware/design/` ändern. Keine Datenblattwerte erfinden — jede Konstante mit Quelle im
  Kommentar. Bestehende 37 Prüfungen dürfen nicht abgeschwächt werden (nur die VBAT-Liste ergänzen).
- Deutsche Prüfnamen wie bisher (ohne Umlaute, z. B. `Schutz-Serienkette`, `Schutz-Schwellen`,
  `Schutz-Ueberstrom`), Fehlschlag-Format `  - <Name>: Ist ..., Soll ...` beibehalten.
