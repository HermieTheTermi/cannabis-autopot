# AUFGABE: Plan für Blatt **A1** neu rechnen (mehr Platz) + Doku A2→A1

## Lage (14.09.2026)

Das Blatt ist live von **A2 auf A1** umgestellt worden (`sch sheet-geometry`:
**3304 × 2338** raw). Grund: mit 12 Modulen und 80 Bauteilen (viele Stecker mit langen
Netznamen) war der ERWEITERUNG-Block auf A2 zu eng — live ergab `sch clusters` 6 × out-of-sheet
(linke Kanten bis −92) und 3 kleine Überlappungen. A1 gibt ~1,4× Fläche pro Achse
(Nutzbereich jetzt x 12…3292, y 12…2326, Titelblock-Keepout ab x ≥ 2602, y ≤ 198).

Gemessene Bauteil-Volumen (live, inkl. eigener Marker/Stiche) liegen weiter vor:
`hardware/easyeda/raw/measured_volumes_2026-09-14.json` — **diese als Platzbedarf verwenden**
(nicht schätzen). Nutzbereich in der Datei muss auf A1 nachgezogen werden.

## Aufgabe

1. **`plan_layout.py` auf A1 rechnen**: `SHEET`-Konstante auf `[0,0,3304,2338]` (Blatt) bzw.
   Nutzbereich x 12…3292, y 12…2326; Titelblock-Keepout x ≥ 2602 **und** y ≤ 198 freihalten.
   - Die 12 Blöcke neu zuschneiden (dürfen größer werden, sich aber nicht überlappen) und die
     Bauteile mit ihren **gemessenen Volumen** darin packen, mit **≥ 25 Abstand** zwischen zwei
     Volumen und **≥ 150 Innenabstand** zwischen Blockrahmen und Inhalt (Reserve: die
     Fächerrichtung wählt erst der Autoconnect-Solver live — genau daran ist A2 gescheitert).
   - Blatt bleibt **eine Seite** (kein Seitenwechsel).
2. **`check_plan_fit.py` auf A1 nachziehen** (neue Nutzgrenzen) und die Schwellen aus 1. prüfen
   (Abstand ≥ 25, Rahmenrand ≥ 150 bzw. ≥ 20 als harte Grenze). Gegenprobe: ein künstlich
   verschobenes Volumen muss Exit 1 liefern. Ergebnis als Rohzahlen berichten.
3. **`raw/` neu erzeugen** (nur Dateien, **keine Live-Aufrufe**):
   ```
   cd hardware/easyeda
   python3 scripts/build_ir.py && python3 scripts/plan_layout.py && python3 scripts/build_autoconnect.py && python3 scripts/netclass_spec.py
   python3 scripts/check_plan_fit.py     # Exit 0
   ```
   Unverändert erwartet: `raw/ir_report.txt` **80 Bauteile / 50 Netze**,
   `check_ir_netlist.py` **0 Abweichungen**, `hardware/design/report.py` **29/29**,
   `check_netlist.py` und `check_bom_consistency.py` Exit 0.
4. **Blattgröße in den Dokumenten nachziehen** (A2 → A1, sonst nichts ändern):
   `hardware/easyeda/README.md`, `docs/09_easyeda-schaltplan-uebergabe.md`,
   `hardware/schaltplan_v1.md`, `docs/02_architektur-und-geometrie.md`, `README.md` — nur dort,
   wo die Blattgröße/„A2" tatsächlich steht (grep nach `A2`); Formulierungen wie
   „auf einem A1-Blatt" bzw. „3304 × 2338 raw" verwenden.
5. **Bericht:** alle 12 Block-Rechtecke, Blattauslastung in %, kleinster Volumen-Abstand,
   kleinster Rahmenrand, Ergebnis von `check_plan_fit.py` (roh), plus ehrliche Restpunkte.

## Regeln

- **Nicht anfassen:** `hardware/schaltplan_v1_netzliste.csv`, `hardware/pcba_bom_jlc.csv`,
  `hardware/design/**`, `docs/06`, `docs/08`, alles außerhalb der genannten Dateien.
- **Kein `git commit`/`push`.** **Keine Live-Aufrufe der `easyeda`-CLI** — das Blatt wird vom
  Koordinator danach neu aufgebaut.
- Keine neuen Bauteile, keine Netze ändern, kein Over-Engineering.
