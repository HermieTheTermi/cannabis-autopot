# AUFGABE: Nachbesserung EasyEDA-Generatoren — echte UUIDs + Lötpads TP7–TP11 wirklich platzieren

## 1. Geräte-UUIDs der neuen Bauteile sind jetzt live aufgelöst (Platzhalter ersetzen)

`easyeda --project "SmartGrowTopf_V1" lib by-lcsc --lcsc C157925,C15127` hat geliefert
(live, 13.09.2026, Bibliothek `0819f05c4eef4c71ace90d822a990e87`):

| LCSC | Hersteller-Typ | device uuid | Footprint |
|---|---|---|---|
| **C157925** (J8) | JST S4B-XH-A(LF)(SN) | **`a65b5fe9c5534a8fbb37396bd1bbe8a2`** | `CONN-TH_S4B-XH-A-LF-SN` |
| **C15127** (Q2) | AOS AO3401A | **`f58385f66b144586baef3753ba84f65d`** | `SOT-23_L2.9-W1.3-P1.90-LS2.4-BR` |

Die deterministisch abgeleiteten Platzhalter-UUIDs in `build_ir.py` (`NEW_PARTS`) sind durch diese
echten Werte zu ersetzen. Kommentar dazu: die Ableitung kann bleiben, falls je ein Bauteil
nachkommt — aber die tatsächlichen UUIDs müssen Vorrang haben.

## 2. TP7–TP11 **müssen** platziert und verdrahtet werden

Die aktuelle Fassung schiebt TP7–TP11 per `raw/no_place.json` aus Platzierung **und** Autoconnect
heraus — Begründung war die IR-Zählung. Das widerspricht dem Zweck: der Nutzer will diese GPIOs
**als Lötpads auf der Platine** haben. Ohne Schemapad gibt es kein PCB-Pad.

**Vorgehen wie bei TP1–TP6** (die sind bereits platziert, gleiche Bauart):
- Geräte-UUID des Testpunkt-Symbols wie TP1–TP6: **`1d9ad61565194f66a2bb1c832c938c3d`**
- `raw/no_place.json` auf die tatsächlich ausgeschlossenen Teile reduzieren (TP7–TP11 entfernen)
- in `plan_layout.py`/`place_all.sh` mit sinnvollen Koordinaten im MCU-Block platzieren
  (nicht überlappend, Pin-Fan von U1 beachten — lieber etwas mehr Abstand)
- in die Autoconnect-Specs aufnehmen (`kind: netport`), Netze laut Netzliste:

| Pad | Netz | Gegenstück |
|---|---|---|
| TP7 | `SPARE_IO15` | U1 Pin 20 (IO15) |
| TP8 | `UART_TX` | U1 Pin 31 (TXD0), dort auch R_UART/TP1 |
| TP9 | `UART_RX` | U1 Pin 30 (RXD0), dort auch TP2 |
| TP10 | `SPARE_IO22` | U1 Pin 28 (IO22) |
| TP11 | `SPARE_IO23` | U1 Pin 29 (IO23) |

## 3. Nachweis (erneut belegen)

- Alle Generatoren laufen mit `python3` durch, Exit 0, **keine Live-CLI-Aufrufe**.
- Selbstkonsistenzprüfung IR ↔ `hardware/schaltplan_v1_netzliste.csv`: **0 Abweichungen**,
  und diesmal **inkl. TP7–TP11** (Zahl der Verbindungen nennen — vorher 190 mit 23 übersprungen).
- `raw/place_all.sh` enthält **75** Platzierungszeilen (70 + 5 Lötpads) — Zahl belegen.
- Kurzbericht: was geändert, welche Zahlen, was weiterhin offen ist.

## Regeln

- Nur Dateien unter `hardware/easyeda/` ändern. Keine Live-Aufrufe der `easyeda`-CLI.
- Ehrlich melden, wenn eine Zählung nicht aufgeht — nicht durch Ausschluss von Bauteilen glätten.
