# AUFGABE (klein): Sicherung F1 in Prüfungen und Linter aufnehmen

## Lage
Die Review-Korrekturen sind eingearbeitet. Neu in der Schaltung:
- **F1**: Littelfuse **0452005.MRL**, 5 A träge, Bauform 2410, **125 V AC/DC**, I²t 53,7 A²s
  (LCSC `C66503`). Liegt in der **Pack-Plus**-Leitung: Netz `PACK_PLUS` (J1 Pin 3) → F1 → Netz `VBAT`.
- Ebenfalls neu: **J18** (JST-XH-2P, Akku-NTC) und **R_NTC_PAR** (82 kΩ, 0805).

## Zwei kleine Änderungen

### 1. `hardware/design/checks.py` — F1 in die VBAT-Spannungsfestigkeit aufnehmen
`python3 hardware/design/report.py` läuft 41/42:

```
- VBAT-Spannungsfestigkeit: Ist 13 Teilnehmer: C_B3_IN, …, F1, R3a, … -> F1 nicht als VBAT-tauglich
  gelistet, Soll nur zugelassene Teilnehmer, V_IN,max >= 8,4 V
```

Die Prüfung hat eine Positivliste der Bauteile, die am Netz `VBAT` liegen dürfen. **F1 gehört hinein** —
belegt ist 125 V AC/DC (Datenblatt Littelfuse 452-Serie). Bitte die Liste um F1 ergänzen und die
Begründung/den Kommentar an der Stelle entsprechend setzen (Spannungsfestigkeit 125 V ≫ 8,4 V).
Danach muss die Prüfung bestehen: **42 von 42, Exit 0.**

### 2. `scripts/check_bom_consistency.py` — Designator-Vorsilbe `F` zulassen
Der Linter kennt nur die Präfixe `U|C|R|D|Q|J|L|SW` (Konstante `DESIG`). Dadurch meldet er
fälschlich:

```
✗ FEHLT IM SCHALTPLAN: F1 (JLC-BOM: 5A traege 2410)
```

`F` (Sicherung) in das Muster aufnehmen (F1, F2, …). Danach muss der Abgleich grün sein:
`Schaltplan: 116 Bauteile · JLC-BOM: 116 Bauteile` und „✓ Schaltplan und JLC-BOM sind konsistent."

**Achtung:** In dieser Datei nichts anderes ändern (kein Umbau der Prüflogik).

## Abnahme (mit echten Ausgaben belegen)
1. `python3 hardware/design/report.py` → **42 von 42, Exit 0**, keine Fehlschlag-Zeile.
2. `python3 scripts/check_netlist.py hardware/schaltplan_v1_netzliste.csv` → „✓ Keine strukturellen Befunde."
3. `python3 scripts/check_bom_consistency.py hardware/pcba_bom_jlc.csv hardware/schaltplan_v1_netzliste.csv` → konsistent, **116 Bauteile** auf beiden Seiten.
4. Beide Mutationssätze erneut fahren (`hardware/design/mutation_cases_2s.json`, `mutation_cases_schutz.json`) und das Ergebnis nennen.
5. `git status --short` — es dürfen nur `hardware/design/` und `scripts/check_bom_consistency.py` geändert sein.

Nichts committen, nur die Dateien ändern. Antwort auf Deutsch, kurz.
