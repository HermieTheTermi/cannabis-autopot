# AUFGABE: Rückbau der GPIO-Erweiterung (J8/J9/J10/Q2/I²C/Reserve) aus dem Projekt

## Ausgangslage (kurz)

Der Commit `466ad9b` (Sonntag, 13.09.2026, 22:20) hat **vier** Dinge in einem Rutsch gemacht:

1. **Lichtsensor** (J7 + R_LIGHT + R_LIGHT_S + C_LIGHT, Netze LIGHT_RAW/LIGHT_RAW_S/LIGHT_AOUT, Modul `LICHT`)
2. **Pinordnung GND–VCC–SIG** aller 3-poligen Stecker (J2 und J7: 1 = GND, **2 = VCC**, 3 = SIG)
3. **U1-Massepads 36–53 einzeln verdrahtet** (Netzlisten-Sammelzeile aufgelöst)
4. **GPIO-Erweiterung**: J8 (I²C, 4-pol) + Q2 P-Kanal-Load-Switch + R_GATE/R_SDA_S/R_SCL_S/R_SDA_PU/R_SCL_PU,
   J9 (Reserve-Analog) + R_SPARE_AIN + C_SPARE, J10 (Reserve-Digital) + R_SPARE_IO, TP7–TP11 als Lötpads
   → Modul `ERWEITERUNG` (17 Bauteile, 13 Netze)

**Der Nutzer will Position 1, 2 und 3 behalten und Position 4 vollständig zurückbauen.**

## Die Netzliste ist die Vorgabe — NICHT ÄNDERN

`hardware/schaltplan_v1_netzliste.csv` ist **bereits** zurückgebaut (42 Zeilen entfernt; der Koordinator hat
das gemacht). Sie enthält jetzt **58 Bauteile und 32 Netze** und ist die einzige Wahrheit.
**Diese Datei darf NICHT editiert werden.** Alles andere wird an sie angepasst.

**Ebenfalls schon angepasst (nicht mehr anfassen):** `hardware/pcba_bom_jlc.csv` (52 bestückte Positionen,
Zeilenzusammenfassungen wie `JST-XH-3P,J2 J7`, `100nF,… 8`, `10k,… 5`, `1k,… 4`).

### Zu entfernende Bauteile
Funktional: `J8`, `J9`, `J10`, `Q2`, `R_GATE`, `R_SDA_S`, `R_SDA_PU`, `R_SCL_S`, `R_SCL_PU`,
`R_SPARE_AIN`, `R_SPARE_IO`, `C_SPARE`, `TP7`, `TP8`, `TP9`, `TP10`, `TP11`
Numerisch (aus `raw/designator_changes.json`): R19, R20, R21, R22, R23, R24, R25, C16 — die restlichen
Refdes bleiben **unverändert** (R17/R18/C15/J7 bleiben; Lücken in der Nummerierung sind ausdrücklich ok).

### Zu entfernende Netze
`SDA`, `SDA_MCU`, `SCL`, `SCL_MCU`, `VCC_EXT`, `EXT_EN`, `SPARE_AIN_RAW`, `SPARE_AIN`,
`SPARE_IO_RAW`, `SPARE_IO`, `SPARE_IO15`, `SPARE_IO22`, `SPARE_IO23`
(→ es bleiben **32 Netze**.)

### Das Modul `ERWEITERUNG` entfällt komplett
Es bleiben **11 Module**: AKKU, DEBUG, LADER, LDO, LICHT, MCU, PUMPE, SENSOR, TASTER, USB, WAEChTER.

## Zielzahlen — müssen in ALLEN genannten Dokumenten so stehen

| Größe | neu |
|---|---|
| Bauteile im Schaltplan | **58** (inkl. TP1–TP6 und J6) |
| Netze | **32** |
| Module | **11** |
| Testpunkte | **TP1–TP6** (6) — TP7–TP11 gibt es nicht mehr |
| Bestückte Positionen (JLC-BOM) | **52** |
| Prüfungen `hardware/design/report.py` | **24 von 24** (heute 29) |
| Blatt | A2 bleibt A2 |

## Aufgaben

1. **`hardware/schaltplan_v1.md`** — alle Zeilen, Tabellenzeilen und Abschnitte zu J8/J9/J10/Q2/Load-Switch/
   VCC_EXT/I²C/Reserve/TP7–TP11 entfernen (Netztabelle, BOM-Tabelle, Stecker-Tabelle, Blockbild, Logik-Ein-/
   Ausgänge, Auslegungstexte, offene Punkte). Dabei **behalten**:
   - den kompletten Lichtsensor-Block (J7/R_LIGHT/R_LIGHT_S/C_LIGHT, LIGHT_RAW/LIGHT_AOUT),
   - den Pinordnungs-Absatz **GND–VCC–SIG**, aber reduziert auf die Stecker **J2 und J7**
     (Sätze über J9/J10 streichen),
   - die Aussage zu den einzeln verdrahteten U1-Massepads 36–53,
   - die Pin-Korrektur zu IO4/IO5 (nicht boot-kritisch).
   In der Rückschau/Chronologie am Ende des Dokuments eine Zeile ergänzen:
   „14.09.2026: GPIO-Erweiterung (J8/J9/J10/Q2/TP7–TP11) auf Wunsch des Nutzers wieder entfernt.“
2. **`docs/01_anforderungen.md`** — Erweiterungsstecker-/I²C-/Load-Switch-Sätze entfernen; der Absatz zur
   Pinordnung bleibt (auf J2/J7 reduziert). Firmware-Regeln zum Licht bleiben.
3. **`docs/02_architektur-und-geometrie.md`** — den Abschnitt **„Erweiterungs-Kabelaustritte"** entfernen und
   die Kanal-Aussage auf den realen Stand zurücknehmen: es gehen **nur Feuchtesensor-Kabel (J2),
   Lichtsensor-Kabel (J7) und die Taster-Rückleitung** durch → die alte Ø-4-mm-Kanal-Annahme bleibt gültig
   (plus Lichtsensor-Kabel, ggf. ein Hinweis „zwei dünne Sensorleitungen im vorhandenen Kanal").
   Keine vier Kabelaustritte, kein I²C-Kabel. Checkliste am Ende entsprechend bereinigen.
4. **`docs/09_easyeda-schaltplan-uebergabe.md`** — Zahlen aktualisieren: 45 → **32** Netze,
   64 → **52** bestückte Positionen, TP1–TP11 → **TP1–TP6**, 29/29 → **24/24**.
5. **`docs/10_pcb-leiterbahnbreiten.md`** — „alle 45 Netze (inkl. I²C/VCC_EXT/Reserve)" → **32 Netze**.
6. **`README.md`** (Projektwurzel) — Zahlen und Erweiterungs-Erwähnungen angleichen.
7. **`hardware/easyeda/README.md`** — Zahlen und Beschreibung (Erweiterung raus, Lichtsensor bleibt).
8. **`hardware/pcba_verfuegbarkeit_jlc.md`** — Zeilen für J8, Q2, J9, J10, C_SPARE, R_SDA_PU, R_SCL_PU,
   R_SDA_S, R_SCL_S, R_SPARE_AIN, R_SPARE_IO entfernen; Stückzahlen an `pcba_bom_jlc.csv` angleichen
   (JST-XH-3P 4 → 2; 100 nF 9 → 8; 47 k 2 → 1; 10 k 7 → 5; 1 k 8 → 4; JST-XH-4P-Zeile entfällt).
9. **`research/smart-grow-topf-esp32-firmware-konzept.md`** — Abschnitt **§4c I²C-Erweiterungsbus (J8)** und
   alle Sätze zu EXT_EN/IO20/VCC_EXT/J8 entfernen. Die historischen XIAO-/OLED-Notizen (anderer Zeitstand)
   bleiben unverändert.
10. **`hardware/design/checks.py`** — alle Prüfungen und Pin-Tabellen zur Erweiterung entfernen
    (`Erweiterungs-Pins`, `I2C-Pull-ups`, `I2C-Module plausibel`, die IO18/IO19-Pinmap-Zeilen, alles was
    J8/J9/J10/Q2/VCC_EXT/EXT_EN/SPARE betrifft). Ergebnis: `python3 hardware/design/report.py` → **24/24**.
11. **`hardware/design/MUTATIONSTEST.md`** — Aussagen zur Anzahl der Prüfungen und zur Erweiterung
    korrigieren (die Erweiterungs-Mutationen entfallen). Ehrliche Zahlen.
12. **`hardware/easyeda/scripts/build_ir.py`** — die 17 COMPS-Einträge der Erweiterung entfernen,
    Kommentare dazu anpassen (TP7–TP11 existieren nicht mehr; die Lötpad-Sonderbehandlung auf TP1–TP6/J6
    beziehen).
13. **`hardware/easyeda/scripts/plan_layout.py`** — Block `'ERWEITERUNG'` und alle Erweiterungs-Kommentare/
    Reihenfolgeeinträge entfernen.
14. **`hardware/easyeda/s0_spec.json`** — Zone „Erweiterung (I2C + Reserve)", Stecker-Eintrag J8 und
    zugehörige Pin-Absichten entfernen.
15. **`raw/`-Artefakte neu erzeugen** (nur Dateien schreiben, **keine Live-Aufrufe der `easyeda`-CLI**), in
    dieser Reihenfolge:
    ```
    cd hardware/easyeda
    python3 scripts/build_ir.py          # raw/ir_draft.json, raw/ir_numbered.json, raw/no_place.json, raw/ir_report.txt
    python3 scripts/plan_layout.py       # raw/placement.json, raw/layout_input.json, raw/place_all.sh, raw/frames.json
    python3 scripts/build_autoconnect.py # raw/ac_*.json, raw/ac_summary.txt
    python3 scripts/netclass_spec.py     # raw/../netclass_spec.json
    ```
    - `raw/ac_ERWEITERUNG.json` **löschen**
    - `raw/designator_changes.json`: die 9 Einträge der Erweiterungs-Bauteile entfernen
    - `raw/modules.json`: die 17 Erweiterungs-Einträge entfernen
    - `raw/no_place.json`, `raw/prefixes.json` und die `place_*.json`/`li_excl_*.json` nur so weit anpassen,
      wie die Generatoren sie selbst erzeugen; keine Handarbeit an Zwischenständen, die kein Generator liest.
    - `raw/ir_report.txt` muss am Ende **58 Bauteile / 32 Netze** ausweisen.
16. **Prüfer laufen lassen und die exakten Zahlen berichten** (Rohzahlen, nicht gerundet):
    ```
    python3 hardware/design/report.py                      # Erwartung: 24 von 24, Exit 0
    python3 scripts/check_netlist.py                        # Erwartung: Exit 0, keine Befunde
    python3 scripts/check_bom_consistency.py                # Erwartung: Exit 0, Schaltplan == JLC-BOM == 52
    python3 hardware/easyeda/scripts/check_ir_netlist.py    # Erwartung: 0 Abweichungen IR <-> Netzliste
    ```
    Wenn eine Zahl **nicht** passt: melden, nicht schönrechnen.

## Regeln

- **`hardware/schaltplan_v1_netzliste.csv` und `hardware/pcba_bom_jlc.csv` NICHT anfassen.**
- **Keine Live-Aufrufe der `easyeda`-CLI** — nur Dateien im Repo bearbeiten/erzeugen.
- **Kein `git commit`, kein `git push`** — der Koordinator prüft und committet selbst.
- Keine neuen Funktionen, keine Umbenennung verbleibender Bauteile, keine Umstrukturierung.
- `docs/06_review-schaltplan.md` und `docs/08_review-schaltplan-3.md` sind historische Momentaufnahmen
  → **unverändert lassen**.
- Am Ende darf im gesamten Repo **keine inhaltliche Erwähnung** der Erweiterung mehr übrig sein
  (außer der Chronologie-Zeile in `hardware/schaltplan_v1.md` und den historischen Reviews).
- Kurzbericht am Ende: geänderte Dateien, Prüfergebnisse als Rohzahlen, offene Punkte.
