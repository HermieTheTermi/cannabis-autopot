# AUFGABE: EasyEDA-Kette auf den Review-Stand ziehen (nur scripts/ + raw/ in hardware/easyeda/)

## Lage
Nach einem externen Review wurde die Schaltung **lokal** korrigiert (Netzliste, `hardware/schaltplan_v1.md`,
JLC-BOM sind fertig und konsistent: **122 Bauteile, 76 Netze, 342 Verbindungen**, beide Projekt-Linter
grün, Design-Suite 42/42). Die EasyEDA-Kette steht noch auf dem alten Stand und muss nachziehen.

**Diese Dateien NICHT anfassen:** `hardware/schaltplan_v1_netzliste.csv`, `hardware/schaltplan_v1.md`,
`hardware/pcba_bom_jlc.csv`, `hardware/design/*`, `scripts/check_*`.

## Was schon vorbereitet ist (durch den Koordinator, bitte nur benutzen)
- `raw/lcsc_map.json` — erweitert um **C66503** (Sicherung, uuid `d262e8022e074620918c67a5e445d247`) und
  **C17840** (82 kΩ, uuid `ac8f85262a25459e9477d309c601e85b`).
- `raw/probe_review_2026-09-16.json` — gemessene Pin-Tabellen der beiden neuen Geräte
  (Sicherung: Pins 1/2; 82 kΩ: Pins 1/2), im selben Format wie die übrigen `probe_*.json`.
- `raw/prefixes.json` — um die Bibliotheks-Präfixe der beiden Geräte ergänzt
  (**Sicherung ⇒ `U?`**, 82 kΩ ⇒ `R?`).

## Aufgabe 1: `scripts/build_ir.py` — `COMP_META` pflegen
Aufgenommen werden die **fünf neuen Bauteile** (exakt diese Modul-/Rollentexte):

| Ref | Modul | Rolle |
|---|---|---|
| `F1` | `AKKU` | `Sicherung in der Pack-Plus-Leitung, 5 A traege (2410)` |
| `J18` | `LADER` | `Stecker fuer den Akku-Temperatursensor (XH-2P)` |
| `R_NTC_PAR` | `LADER` | `82 kOhm parallel zum Akku-NTC (Datenblatt-Schwellen 0/45/55 Grad C)` |
| `Q_SENS` | `SENSOR` | `P-Kanal-Lastschalter der Sensorversorgung` |
| `R_SENS_GATE` | `SENSOR` | `47 kOhm Gate-Pull-up des Sensor-Lastschalters` |

**Entfernen** (existieren in der Netzliste nicht mehr, sonst bleiben sie als Leichen im Modell):
`R_FB3_TOP`, `R_FB3_BOT`, `D3`, `D8`, `R_CLAMP1`, `R_CLAMP2`, `R_NTC`.

## Aufgabe 2: Kette durchlaufen lassen (Reihenfolge einhalten)
1. `python3 scripts/build_ir.py` — muss **ohne „kein Modul/Rolle"-Meldung** durchlaufen und
   **122 Bauteile / 76 Netze / 342 Verbindungen** ergeben.
2. `python3 scripts/check_ir_netlist.py` — **0 Abweichungen**.
3. **Designator-Allokation:** `easyeda sch designators allocate raw/ir_draft.json --prefixes raw/prefixes.json --out raw/ir_numbered.json --changes raw/designator_changes.json`
   → danach `raw/ir_numbered.json` auf **0 funktionale Designatoren** prüfen.
4. `python3 scripts/build_ir.py` erneut (zieht die Nummerierung nach), dann
   `python3 scripts/plan_layout.py` → **kein Planungsproblem, Fallback 0**, alle 122 Bauteile,
   `raw/place_all.sh` neu erzeugt (124 → jetzt 122 `sch place`-Zeilen, **nur numerische** Designatoren,
   `--doc 4f6771a27edec75b`, **kein** `2>&1` und **kein** `--doc P1`).
5. `python3 scripts/check_plan_fit.py` → OK.
6. `python3 scripts/build_autoconnect.py` → **14 Dateien**, Verbindungszahl nennen.

## Abnahme (echte Ausgaben belegen)
- Ergebnis von Schritt 1–6 mit den **Zahlen**.
- Insbesondere: **wie viele Verbindungen** ergeben die `ac_*.json` (Soll: 342) und **welche Nummer**
  hat `F1` nach der Allokation bekommen (Bibliotheks-Präfix ist `U?`) — bitte aus
  `raw/designator_changes.json` ablesen und nennen.
- `git status --short`: nur `hardware/easyeda/scripts/`, `hardware/easyeda/raw/` (+ ggf.
  `.opencode_task_*`). Keine Commits.

Ein `opencode`-Lauf auf denselben Dateien läuft bereits — nicht parallel starten.
Antwort auf Deutsch, kurz und mit Zahlen.
