# AUFGABE: Erweiterung neu — freie GPIOs + I²C auf 2,54-mm-Stiftleisten (Steckpins), Doku/Prüfer nachziehen

## Entscheidungen des Nutzers (14.09.2026, nicht abwandeln)

1. Der Lichtsensor-Anschluss und **alle** übrigen freien IO-Pins + I²C werden als
   **2,54-mm-Stiftleiste (male, gerade)** ausgeführt — Kabel mit Dupont-Buchse wird einfach
   draufgesteckt, keine Crimpzange.
2. **Jeder freie GPIO bekommt einen eigenen 3-pol Stecker** (nicht kompakt, nicht 2-reihig).
3. **Pinordnung immer GND–VCC–SIG** (VCC in der Mitte, Pin 2). Beim 4-pol I²C-Stecker:
   **GND–VCC–SDA–SCL** (VCC innen, wie Qwiic/STEMMA).
4. Vom Nutzer bestätigt: die 38-mm-Platine ist eng — das ist bekannt und akzeptiert; die
   **Platzierung macht der Nutzer selbst**, der Schaltplan muss nur die Netze/Stecker enthalten.

## Bereits erledigt (NICHT ANFASSEN)

`hardware/schaltplan_v1_netzliste.csv` und `hardware/pcba_bom_jlc.csv` hat der Koordinator
bereits geändert. Ist-Stand: **80 Bauteile, 50 Netze**, BOM **72** bestückte Positionen.

Neu in der Netzliste (funktionale Namen): `J8` (I²C 4-pol), `J9`…`J15` (je 3-pol, GND–VCC–SIG),
`Q2` + `R_GATE` (Load-Switch für VCC_EXT), `R_SDA_S`/`R_SCL_S` (1 kΩ in Reihe),
`R_SDA_PU`/`R_SCL_PU` (10 kΩ an **VCC_EXT**), `R_SPARE_AIN`, `R_SPARE_IO15/16/17/21/22/23`
(1 kΩ in Reihe), `C_SPARE` (100 nF Filter an IO5).
Netze: `SDA`, `SDA_MCU`, `SCL`, `SCL_MCU`, `VCC_EXT`, `EXT_EN`, `SPARE_AIN_RAW`, `SPARE_AIN`,
`SPARE_IO15_RAW`, `SPARE_IO15`, `SPARE_IO16`, `SPARE_IO17`, `SPARE_IO21_RAW`, `SPARE_IO21`,
`SPARE_IO22_RAW`, `SPARE_IO22`, `SPARE_IO23_RAW`, `SPARE_IO23`
(IO16/IO17 sitzen auf `UART_TX`/`UART_RX`, dort kommt nur der Serienwiderstand dazu).

## Neue/geänderte Bauteile (Verfügbarkeit + Device-Identität live geprüft, 14.09.2026)

| Pos | Teil | LCSC | Device-UUID | Footprint |
|---|---|---|---|---|
| J7, J9–J15 | Stiftleiste 1×3 2,54 mm **male gerade** (XFCN `PZ254V-11-03P`) | `C2937625` | `3c2517dd0d3741b3a6071ea8b3f9b7e9` | `HDR-TH_3P-P2.54-V-M_PZ254V-11-03P` |
| J8 | Stiftleiste 1×4 2,54 mm **male gerade** (XFCN `PZ254V-11-04P`) | `C2691448` | `f70eda5267b34d9799800488a27be0c6` | `HDR-TH_4P-P2.54-V-M` |
| Q2 | AO3401A P-MOSFET | `C15127` | `f58385f66b144586baef3753ba84f65d` | `SOT-23_L2.9-W1.3-P1.90-LS2.4-BR` |

Library-UUID immer `0819f05c4eef4c71ace90d822a990e87`.
**J7 wechselt den Typ**: vorher JST-XH 3P (`C157928`), jetzt dieselbe 1×3-Stiftleiste wie die
Reserve-Stecker (BOM: `JST-XH-3P` nur noch für `J2`).
`TP7`–`TP11` bleiben **entfernt** (durch die Stecker ersetzt); `TP1`/`TP2` (UART) bleiben.

## Aufgaben

1. **`hardware/design/circuit.py` / `checks.py` / `report.py` / `sim.py`** — die neuen Bauteile
   und Netze abbilden und die passenden Prüfungen **wieder aufnehmen** (heute: 24/24):
   - Pinordnung **GND–VCC–SIG** für **alle** 3-pol Stecker (J7, J9–J15): Pin 1 = GND,
     Pin 2 = geschaltete Versorgung, Pin 3 = Signal; 4-pol J8 = GND–VCC–SDA–SCL.
   - **1 kΩ in Reihe in JEDER Signalleitung** zwischen Stecker und MCU
     (R_SDA_S, R_SCL_S, R_SPARE_AIN, R_SPARE_IO15/16/17/21/22/23) — Schutz des Pins.
   - **Load-Switch-Fail-safe:** Q2 Gate über 47 kΩ auf +3V3 (Quellpotential), IO20 zieht nach
     unten ⇒ VCC_EXT ist **beim Reset aus**; keine Stecker-Versorgung direkt von einem GPIO.
   - **I²C-Pull-ups an VCC_EXT**, nicht an +3V3 (kein Busstrom im Aus-Zustand).
   - `C_SPARE` 100 nF ADC-Filter an IO5.
   - Kein Pin doppelt belegt (IO/ADC-Kanal-Prüfung), NC-Pins unverändert.
   Ziel: `python3 hardware/design/report.py` → **alle Prüfungen grün, Exit 0** (Zahl nennen).
2. **`hardware/design/MUTATIONSTEST.md`** — die wieder aufgenommenen Prüfungen mit Mutations-
   nachweis ergänzen (mindestens: Pinordnung verdreht, Serienwiderstand entfernt,
   Pull-up an +3V3 statt VCC_EXT, Gate-Pull-up entfernt). Ehrliche Zahlen.
3. **`hardware/easyeda/scripts/build_ir.py`** — neue COMPS-Einträge (8 Stecker, Q2, 12 R, 1 C)
   mit Modul `ERWEITERUNG`, Designator-Mapping ergänzen (Vorschlag, an die gemessenen
   Library-Präfixe anpassen und im Bericht nennen):
   `R_SDA_PU→R19, R_SCL_PU→R20, R_SDA_S→R21, R_SCL_S→R22, R_GATE→R23, R_SPARE_AIN→R24,
   R_SPARE_IO15→R25, R_SPARE_IO16→R26, R_SPARE_IO17→R27, R_SPARE_IO21→R28, R_SPARE_IO22→R29,
   R_SPARE_IO23→R30, C_SPARE→C16`; Stecker J8–J15 behalten ihre Nummern.
   **Präfix vorher messen** (`easyeda lib device get` / `--include-device-identity`): wenn das
   Stiftleisten-Gerät nicht `J?` als Präfix hat, den gemessenen Präfix nehmen und im Bericht sagen.
4. **`hardware/easyeda/scripts/plan_layout.py`** — Block `'ERWEITERUNG'` (Titel z. B.
   „Erweiterung: Stiftleisten (GND–VCC–SIG) + Load-Switch") mit allen neuen Bauteilen anlegen,
   so dass die Gruppe im Reihenpacker des A2-Blatts Platz findet (Blatt bleibt **A2**).
   `raw/frames.json` muss danach **13 Rahmen** enthalten (unverändert: die bisherigen 12 ohne
   `frame-erweiterung`? Nein — **alle 13 inkl. neuer ERWEITERUNG-Rahmen**; der USB-Rahmen usw.
   bleiben exakt so, wie sie sind).
5. **`hardware/easyeda/scripts/build_autoconnect.py`** + `netclass_spec.py` + `s0_spec.json`
   nachziehen (50 Netze; Breiten für VCC_EXT/SDA/SCL/Reserve wie gehabt: Signale 0,25 mm,
   VCC_EXT 0,5 mm).
6. **`raw/` neu erzeugen** (nur Dateien, **keine** Live-Aufrufe):
   ```
   cd hardware/easyeda
   python3 scripts/build_ir.py && python3 scripts/plan_layout.py && python3 scripts/build_autoconnect.py && python3 scripts/netclass_spec.py
   ```
   Erwartung: `raw/ir_report.txt` → **80 Bauteile, 50 Netze**; `raw/ac_ERWEITERUNG.json` wieder
   vorhanden; `raw/modules.json` mit 12 Modulen.
7. **Dokumente nachziehen** (Zahlen 80 Bauteile / 50 Netze / 72 BOM-Positionen, Prüfungszahl):
   - `hardware/schaltplan_v1.md` — §9 wieder auf „Pinordnung **und** Steckpins" erweitern:
     Tabelle J7/J8/J9–J15 mit Pinbelegung, Load-Switch-Begründung (aus beim Reset),
     Serienwiderstände, Pull-ups an VCC_EXT; Netztabelle, BOM-Tabelle, Stecker-Tabelle,
     Blockbild und Pinbelegungs-Tabelle aktualisieren; Chronologie-Zeile ergänzen:
     „14.09.2026: GPIO-Erweiterung auf 2,54-mm-Stiftleisten wieder eingebaut (je Signal ein
     3-pol GND–VCC–SIG-Stecker, I²C als 4-pol).“
   - `docs/01_anforderungen.md`, `docs/02_architektur-und-geometrie.md` (Kabelaustritte: jetzt
     **9 Kabel** — Feuchte, Licht, I²C + 7 Reserve + Taster — als offener Gehäuse-Punkt),
     `docs/09_easyeda-schaltplan-uebergabe.md`, `docs/10_pcb-leiterbahnbreiten.md`, `README.md`,
     `hardware/easyeda/README.md`, `hardware/pcba_verfuegbarkeit_jlc.md` (neue Positionen
     `C2937625`, `C2691448`, `C15127` aufnehmen, JST-XH-3P auf 1 runter).
   - `research/smart-grow-topf-esp32-firmware-konzept.md` — Firmware-Abschnitt „I²C/Erweiterung"
     wieder aufnehmen (IO20 = VCC_EXT an/aus, Load-Switch-Polarität, IO5 = Reserve-ADC).
8. **Prüfer laufen lassen und Rohzahlen berichten:**
   ```
   python3 hardware/design/report.py
   python3 scripts/check_netlist.py
   python3 scripts/check_bom_consistency.py
   python3 hardware/easyeda/scripts/check_ir_netlist.py     # IR <-> Netzliste, 0 Abweichungen erwartet
   ```

## Regeln

- **`hardware/schaltplan_v1_netzliste.csv` und `hardware/pcba_bom_jlc.csv` NICHT ändern.**
- **Keine Live-Aufrufe der `easyeda`-CLI** — nur Dateien schreiben. Den Live-Neuaufbau des
  Blatts (sch clear → place → frames → autoconnect) macht der Koordinator danach selbst.
- **Kein `git commit`, kein `git push`.** Der Koordinator prüft und committet.
- Lichtsensor (J7 → jetzt Stiftleiste), Pinordnung GND–VCC–SIG, die einzeln verdrahteten
  U1-Massepads und alles Bestehende bleiben erhalten. `docs/06`/`docs/08` (historische Reviews)
  unverändert lassen.
- Keine anderen Bauteile umbenennen, keine Struktur-Refactors, kein Over-Engineering.
- Kurzbericht: geänderte Dateien, Prüfzahlen (roh), offene Punkte.
