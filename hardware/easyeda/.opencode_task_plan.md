# AUFGABE (kurz): Platzierung auf A1 zum Laufen bringen + --doc-Fehler beheben

## Lage
Die Designator-Allokation ist gelaufen: `raw/ir_numbered.json` hat **124 Refs, 0 funktionale**,
`raw/designator_changes.json` hat **79** Einträge. `check_ir_netlist.py` = 0 Abweichungen,
`build_autoconnect.py` erzeugt 14 `ac_*.json` (351 Verbindungen, alle numerisch) — das ist gut.

**Aber:** `python3 scripts/plan_layout.py` bricht jetzt ab mit
`Block LADER (1477x521) passt nicht aufs Blatt`
→ deshalb stammen `raw/place_all.sh` und `raw/placement.json` noch von **vor** der Allokation
(42 funktionale Refs im Plan, `place_all.sh` mit `--doc P1`).

## Auftrag

### 1. Platzierung zum Laufen bringen
A1-Blatt: 3304×2338, nutzbar `x 12..3292`, `y 12..2326`, Titelblock-Keepout `x≥2602 und y≤198`.
Erlaubte Hebel — in dieser Reihenfolge:
1. **Packung verbessern** (Reihenfolge in `MODULE_ORDER`, zweite Reihe, Blöcke geschickter stapeln)
2. **Kleinere Module zusammenlegen** (z. B. `SCHUTZ` in `AKKU` oder `LICHT`/`TASTER` zusammen) —
   dann aber **alle drei** Stellen nachziehen: `raw/modules.json`, `plan_layout.MODULE_ORDER`+`TITLES`,
   `build_autoconnect.order`
3. Einen zu breiten Block in **zwei Reihen** zeichnen (die Blockgeometrie darf sich ändern, die
   Sheet-Größe nicht — erst wenn gar nichts passt, berichten und Vorschlag machen)

**Nicht** erlaubt: gemessene Volumen künstlich verkleinern (`by_device`/`parts` sind Messwerte);
Blattgröße ändern ohne Rückfrage; Bauteile aus dem Plan lassen.

### 2. `--doc`-Fehler beheben
`plan_layout.py` schreibt `G=(--project "SmartGrowTopf_V1" --doc P1)` — die Seite heißt aber
`Systemuebersicht` und **`--doc P1` läuft ins Leere** (jeder Platzierungsbefehl schlägt fehl, sichtbar
nur als `FAIL <Ref>`, weil die Ausgabe nach /dev/null geht). Nutze die **Seiten-UUID**
`4f6771a27edec75b` (so wie es `frames.json` mit `documentId` schon tut). Zusätzlich: die
`easyeda`-Aufrufe in `place_all.sh` sollen **nicht** komplett nach `/dev/null` schreiben, sondern die
Fehlermeldung auf `stderr` durchlassen (sonst diagnostiziert man blind).

## Abnahme (selbst ausführen, Zahlen berichten)
1. `python3 scripts/plan_layout.py` → läuft durch, **124 Bauteile**, Blockzahl == Modulzahl,
   „kein Planungsproblem", `Fallback-Schätzung: keine`
2. `raw/place_all.sh` enthält **0 funktionale** Designatoren und **kein** `--doc P1`;
   jede Zeile hat genau einen `sch place`-Aufruf für die 124 Refs
3. `raw/placement.json`: 124 Einträge, alle Refs numerisch; kein Bauteil außerhalb
   `x 12..3292 / y 12..2326` und keines im Titelblock-Keepout
4. `python3 scripts/check_plan_fit.py` → OK, „Fallback-Schätzungen: 0"
5. `python3 scripts/build_autoconnect.py` → 14 Dateien, alle Refs numerisch
6. `check_ir_netlist.py` → weiter 0 Abweichungen

## Regeln
Nur `hardware/easyeda/scripts/*` und `raw/*` (Generatordateien). Netzliste, Doku, Design-Suite und
BOM sind Wahrheit. Keine Live-Aufrufe der easyeda-CLI. Designatoren nicht selbst vergeben.
